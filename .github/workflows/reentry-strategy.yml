name: Correction Re-entry Strategy

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/reentry_strategy.py'
      - 'tools/reentry_decision.py'
      - 'tools/reentry_confidence.py'
      - '.github/workflows/reentry-strategy.yml'

concurrency:
  group: swing-intelligence-twelve-data
  cancel-in-progress: false

jobs:
  signal:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install pandas numpy
      - name: Run correction re-entry strategy
        env:
          TWELVE_DATA_API_KEY: ${{ secrets.TWELVE_DATA_API_KEY }}
        run: python tools/reentry_strategy.py
      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: reentry-strategy
          path: artifacts/reentry_strategy/**
          retention-days: 14
